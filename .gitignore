# secrets
.credentials.json
settings.local.json
*.local.json

# machine-specific config (ship settings.example.json instead)
settings.json

# third-party plugins (own repos)
plugins/

# vendored third-party skill content (own repos) — fetched locally by
# /update vendor <name>, never committed; see docs/THIRD_PARTY_SKILLS.md
skills/design/vendor/

# external symlinked skills
skills/improve
skills/impeccable
# emilkowalski/skills — animation/design-eng taste rules (npx skills@latest add emilkowalski/skills)
skills/emil-design-eng
skills/animation-vocabulary
skills/review-animations
# ponytail-* = old name (pre-rename, still installed locally)
skills/ponytail
skills/ponytail-audit
skills/ponytail-debt
skills/ponytail-gain
skills/ponytail-help
skills/ponytail-review
# trim-* = new name (after reinstall from holland-built/trim)
skills/trim
skills/trim-audit
skills/trim-debt
skills/trim-gain
skills/trim-help
skills/trim-review

# lazyweb = externally managed by lazyweb installer (symlinks to ~/.lazyweb/)
skills/lazyweb/
# lazyweb-* = externally managed by lazyweb installer (symlinks to ~/.lazyweb/)
skills/lazyweb-ab-test-research
skills/lazyweb-deep-design-research
skills/lazyweb-design-best-practices
skills/lazyweb-design-brainstorm
skills/lazyweb-design-improve
skills/lazyweb-lite-design-research
skills/lazyweb-optimize-paywall
skills/lazyweb-optimize-sign-up
skills/lazyweb-paywall-cta
skills/lazyweb-quick-search
skills/lazyweb-update
# interface-design = externally managed by npx skills@latest (symlink to ~/.agents/)
skills/interface-design

# runtime / session
projects/
sessions/
session-data/
session-env/
shell-snapshots/
file-history/
ide/
metrics/

# caches
cache/
paste-cache/

# personal / private (project-scoped memories live in projects/ — already ignored)
memory/compile-manifest.json
memory/facts/

# ephemeral work
plans/
advisor-plans/
tasks/
proposals/
brainstorms/

# archives / trash
_archive_*/
backups/
.skill-trash/
skills/*/_claude_bak/
skills/learned/

# logs
history.jsonl
*.log

# runtime markers
.pending-tasks.md
learnings.md
.caveman-active
.last-cleanup
.last-update-result.json
.learnings-turn-counter
.reflect-turn-counter
mcp-health-cache.json
mcp-needs-auth-cache.json

# OS
.DS_Store
.agents/
skills-lock.json

# Claude Code runtime state (created when ~/.claude itself is opened as a project)
.claude.json
.claude/
chrome/
debug/
