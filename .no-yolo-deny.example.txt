# no-yolo deny-list, TEMPLATE. setup.sh copies this to .no-yolo-deny.txt
# (gitignored) on first run; edit that copy, never this one.
#
# One term per line; matching is case-insensitive substring. The pre-commit
# hook refuses any commit whose ADDED lines contain a term from your copy.
# Use it for employer names, internal product names, private hostnames:
# anything that must never reach a public repository.
#
# Lines starting with # are ignored. Replace these placeholders:
# your-company
# internal.example.com
