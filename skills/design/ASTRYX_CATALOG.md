# Astryx component catalog — the Meta UX menu /design and /build reach for

> Meta's open-source React design system (`github.com/facebook/astryx`, MIT). Real, installable
> package: `@astryxdesign/core@0.1.4`. This file exists so the fresh-gen mockup agents and the
> build agents KNOW these components are available and reach for them proactively — not only
> when the user names one.
>
> **This file is GENERATED — never hand-edit the component list below.** Regen with
> `./skills/design/regen-astryx-catalog.sh` (pull-from-source-at-build-time, same principle as
> CORE_RULES rule 9). On any new Astryx release, rerun the script and commit the diff — the
> version stamp below makes staleness visible.
>
> **Never import a name that isn't in this file.** Before any import, confirm the exact export +
> props via `node node_modules/@astryxdesign/core/docs.mjs <Name>` (or
> `npx astryx component <Name>`). If a component isn't here, hand-build — never guess a name.
>
> **Only reach for these in a React + npm/bundler project.** Not React, or React-via-CDN with no
> `package.json` (e.g. a babel-standalone page): do NOT use Astryx — mock the behavior / hand-build.

## When to reach for Astryx (proactive rule)
**Astryx is the GREENFIELD default, not a universal add-on.** Reach for it ONLY when the
sourcing gate (`skills/design/PREFAB_SOURCING.md`) finds NO existing component library in a
React + npm project — or when the project already uses Astryx. A project on
shadcn/Radix/MUI/Chakra/etc. sources every component from THAT library instead; never place
Astryx components beside a different library — mismatched twin systems look broken.

A design "calls for a rich interaction" when it includes any of: a preview-on-hover (**HoverCard**),
live/typeahead search (**Typeahead** / **PowerSearch**), a chat/messaging surface (**Chat**), a
command palette (**CommandPalette**), stacked toasts (**Toast**), a media carousel or fullscreen
viewer (**Carousel** / **Lightbox**), a token/multi-select input (**Tokenizer** / **MultiSelector**),
or app scaffolding/nav (**AppShell** / **SideNav** / **TopNav**). For those, **prefer the finished
Astryx component over hand-building** — it's accessible, animated, and battle-tested. Theme it to
the project's tokens so it looks like the project, not like Facebook. There is no real Astryx
component for an infinite/virtualized feed, emoji reactions, a rich mention/emoji composer, or a
story-bar — **hand-build those explicitly**, don't go hunting for a component that doesn't exist.
For plain/static pieces, hand-build as normal.

## Components (generated from @astryxdesign/core@0.1.4 — do not hand-edit below this line)

### Layout
- **AspectRatio** — Maintains a fixed width-to-height ratio for its children, regardless of scree...
- **Center** — Center aligns content to the middle of its container. Use it for empty states...
- **Grid** — A CSS grid layout container for arranging children in rows and columns. Use G...
- **Layout** — Layout provides composable components for building structured page shells wit...
- **Stack** — Stack arranges items in a row or column with consistent spacing. Use the gap ...

### Display
- **Avatar** — Avatar represents a person or team with a profile photo, initials, or a defau...
- **Badge** — Badge highlights a status or category at a glance. Use it sparingly: only whe...
- **Divider** — A visual separator that divides content into distinct sections. Use to create...
- **Icon** — Icons are small visual symbols that represent actions, objects, or concepts. ...
- **Skeleton** — An animated shimmer placeholder that previews the shape of content while it l...
- **Table** — Table displays structured data in rows and columns with consistent dimensiona...
- **Text** — Text renders styled body text and headings from the theme. Use Text with a se...

### Form
- **CheckboxInput** — CheckboxInput toggles a single on/off value. Use it for settings like "Enable...
- **DateInput** — DateInput lets the user type or pick a date from a calendar popover. Use it f...
- **Field** — Field is a low-level wrapper for custom, native, or third-party controls that...
- **NumberInput** — A form input for numeric values with built-in validation, min/max constraints...
- **RadioList** — A group of options where only one can be selected at a time. All options are ...
- **Selector** — A dropdown selector for choosing a single value from a list of options. Suppo...
- **Slider** — A draggable control for selecting a numeric value or range within defined bou...
- **Switch** — A toggle control for on/off states that take effect immediately. Supports lab...
- **TextArea** — TextArea is a multi-line text input for collecting longer-form content like c...
- **TextInput** — TextInput collects short-form text like names, emails, or search queries. Use...
- **TimeInput** — TimeInput lets users enter a time of day and converts it to a standard format...

### Action
- **Button** — Button triggers an action when clicked. Use it for form submissions, confirma...
- **DropdownMenu** — A dropdown menu that displays a list of actionable items in a popup triggered...
- **Link** — A styled anchor for inline and standalone text navigation. Supports external ...

### Navigation
- **TabList** — TabList provides tab-style navigation for organizing content into categorized...
- **TopNav** — TopNav is a horizontal navigation bar for product-level navigation in applica...

### Overlay
- **Calendar** — Calendar lets the user pick a date or date range from a month grid. Use it in...
- **Dialog** — Dialog displays a modal overlay that blocks interaction with the page until t...
- **Layer** — Layer utilities provide the app-level provider used by overlay systems. Use L...

### Other
- **AlertDialog** — AlertDialog asks the user to confirm a destructive or irreversible action bef...
- **AppShell** — The outermost layout for an application. Provides slots for top navigation, s...
- **AvatarGroup** — AvatarGroup displays multiple avatars in an overlapping row with an optional ...
- **Banner** — Banner shows a persistent message at the top of a page or section. Use it for...
- **Blockquote** — A styled quotation block with an accent-colored left border and secondary tex...
- **Breadcrumbs** — Breadcrumbs show a trail of links from the root to the current page. Use them...
- **ButtonGroup** — ButtonGroup joins related actions into a single connected control. Use it whe...
- **Card** — Card is a bordered, elevated container for discrete, self-contained items: th...
- **Carousel** — Carousel scrolls a row of items horizontally when they overflow the available...
- **Chat** — Chat is a family of composable primitives for building AI and human chat expe...
- **CheckboxList** — CheckboxList shows a small group of checkboxes so users can turn several opti...
- **Citation** — Citations display inline references to external sources. Use them to attribut...
- **ClickableCard** — An interactive card for navigation or action targets. Nested interactive elem...
- **CodeBlock** — CodeBlock renders syntax-highlighted code with line numbers, a copy button, a...
- **Collapsible** — Collapsible hides and reveals content behind a trigger button. Use it in sett...
- **CommandPalette** — CommandPalette is a searchable dialog for quick access to commands, navigatio...
- **ContextMenu** — A right-click context menu that appears at the cursor position. Use to provid...
- **DateRangeInput** — DateRangeInput lets users select a start and end date from a dual-month calen...
- **DateTimeInput** — DateTimeInput combines a calendar popover with a time input for selecting bot...
- **EmptyState** — EmptyState shows a placeholder when a content area has no data. Use it for em...
- **FieldStatus** — FieldStatus renders validation feedback for fields and field-like controls. U...
- **FileInput** — FileInput provides file upload with optional drag-and-drop support. Use it fo...
- **FormLayout** — A layout container that arranges form fields with consistent spacing and dire...
- **HoverCard** — HoverCard shows additional information when the user hovers or focuses a trig...
- **IconButton** — A button that shows only an icon with no visible text. Use IconButton in tool...
- **InputGroup** — InputGroup connects an input with prefix/suffix addons in a single visual uni...
- **Item** — A single, flexible item primitive that unifies the "start content + label + d...
- **Kbd** — Renders a keyboard shortcut as styled key badges. Use Kbd in tooltips, menus,...
- **Lightbox** — A fullscreen overlay for viewing images and videos at full resolution. Suppor...
- **List** — A vertical collection of items with consistent spacing, dividers, and optiona...
- **Markdown** — Renders a markdown string as Astryx-styled components. Use Markdown for user-...
- **MetadataList** — MetadataList displays key-value pairs for object attributes like quality, con...
- **MobileNav** — A slide-out drawer for mobile navigation. MobileNav is the mobile counterpart...
- **MoreMenu** — MoreMenu is a three-dot button that opens a list of actions. Use it for secon...
- **MultiSelector** — A checkbox dropdown for selecting multiple values from a list. Selected items...
- **NavIcon** — NavIcon is a circular icon container with an accent-colored background. Use i...
- **NavHeadingMenu** — Accessible menu container and items for nav heading popovers. NavHeadingMenu ...
- **Outline** — A table-of-contents sidebar for documentation pages, help centers, wikis, and...
- **OverflowList** — A horizontal list that automatically hides items when they exceed the availab...
- **Overlay** — Overlay layers action or supporting content over media, cards, video, or othe...
- **Pagination** — Pagination lets users step through pages of content. Place it below a table, ...
- **Popover** — A click-triggered overlay anchored to a button or trigger element. Use it for...
- **PowerSearch** — PowerSearch is a structured filter bar where each token represents a field, o...
- **ProgressBar** — A horizontal bar showing the completion progress of a task. Use it for operat...
- **Resizable** — Hook-based resizable panel system. useResizable() manages size state and Resi...
- **Section** — Section is the correct way to create page regions and group related content o...
- **SegmentedControl** — A segmented button group that allows users to make a single selection from a ...
- **SelectableCard** — A card that toggles between selected and unselected states with an accent bor...
- **SideNav** — A sidebar navigation component for organizing application pages with sections...
- **Spinner** — An animated loading indicator for processes with unknown duration, such as da...
- **StatusDot** — A small colored dot that communicates status like online/offline presence or ...
- **Thumbnail** — Thumbnail displays a compact, square preview of an image attachment. It shows...
- **Timestamp** — Timestamp formats a date or time value into human-readable text. Use it to sh...
- **Toast** — Toast shows a brief, non-blocking notification to confirm an action or presen...
- **ToggleButton** — ToggleButton switches between selected and unselected states to represent a p...
- **Token** — Token is a small, inline element for representing discrete pieces of associat...
- **Tokenizer** — Tokenizer is a multi-select input that lets users search, select, and manage ...
- **Toolbar** — Toolbar is a horizontal bar with left, center, and right areas. Use it for co...
- **Tooltip** — A short text hint that appears on hover or focus, anchored to a trigger eleme...
- **TreeList** — An expandable tree structure for displaying hierarchical data with branch con...
- **Typeahead** — A searchable input for selecting a single item from a large or dynamic datase...
- **VisuallyHidden** — Renders content in the accessibility tree while hiding it visually. Use for a...
