{
  "permissions": {
    "defaultMode": "auto"
  },
  "skillOverrides": {},
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "LOG=\"$HOME/.claude/reap-codex-orphans.log\"; { date -u '+== %Y-%m-%dT%H:%M:%SZ'; \"$HOME/AI/no-yolo/scripts/reap-codex-orphans.sh\"; } >>\"$LOG\" 2>&1 || echo \"reap-codex-orphans failed; see $LOG\" >&2",
            "timeout": 120,
            "statusMessage": "Reaping orphaned Codex brokers",
            "async": true
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "/Users/sholland/.claude/statusline.sh",
    "padding": 0
  },
  "enabledPlugins": {
    "codex@openai-codex": true
  },
  "outputStyle": "Plain",
  "modelSettings": {
    "claude-opus-5": {
      "effortLevel": "medium"
    }
  },
  "tui": "fullscreen",
  "skipDangerousModePermissionPrompt": true,
  "theme": "dark",
  "inputNeededNotifEnabled": false,
  "agentPushNotifEnabled": true,
  "autoMode": {
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: Not queryable here (no remotes configured on this repo \u2014 `~/.claude` has no git remote); assume private",
      "**Internal sharing / snippet hosting**: None configured \u2014 treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: None configured",
      "**Default / protected branches**: Not queryable here (no remotes configured, default branch unknown)",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Host containment**: None configured \u2014 assume Claude Code runs on an ordinary developer machine with open internet",
      "**Source control**: The trusted repo (`~/.claude`, no configured remotes) only \u2014 no additional orgs configured",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment",
      "**Protected deployment namespaces / environments**: None configured \u2014 fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: software development (personal Claude Code configuration and tooling, per ~/.claude/CLAUDE.md)",
      "**Trusted repo**: `~/.claude` (the working directory this session started in; no remotes configured)",
      "**Org-specific CLIs**: None configured \u2014 user runs `codex`, `cc-work`, `cc-home`, `cc-personal`, `cd-home`, `herdr`, `bloxsmith`, `tofu` from shell history but no corroborating org/trust context was found for these"
    ]
  }
}
